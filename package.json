{
  "name": "echodesk-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Unified entry point for EchoDesk AI frontend and backend",
  "scripts": {
    "bootstrap": "npm install --prefix frontend && npm install --prefix backend/backend && npm install --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "dev:backend": "npm run start --prefix backend/backend",
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
